;#; This file was generated by the MZ compiler.
;#; Works but doesnt print anything
    .section .data
        b: .space 8
        a: .space 8
        fmt: .asciz "%lld"

    .global main

    .section .text

main: 
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp

    lea a(%rip), %rax
    movq $420, (%rax)
    lea b(%rip), %rax
    movq $42, (%rax)

    movq $0, %rax
    lea fmt(%rip), %rdi
    movq $42069, %rsi
    call printf

    add $16, %rsp
    pop %rbp
    movl $0, %ebx
    movl $1, %eax
    int $0x80
